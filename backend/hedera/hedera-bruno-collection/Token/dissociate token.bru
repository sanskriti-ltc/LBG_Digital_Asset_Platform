meta {
  name: dissociate token
  type: http
  seq: 7
}

post {
  url: {{rootUrl}}/token/dissociate
  body: json
  auth: inherit
}

body:json {
  {
    "tokenId": "{{operatorTokenId}}",
    "account": "{{operatorAccountId}}",
    "amount": 100,
    "sender": "operator"
  }
}
