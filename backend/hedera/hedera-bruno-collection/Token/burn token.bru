meta {
  name: burn token
  type: http
  seq: 3
}

post {
  url: {{rootUrl}}/token/burn
  body: json
  auth: inherit
}

body:json {
  {
    "tokenId": "{{operatorTokenId}}",
    "amount": 100,
    "sender": "operator"
  }
}
